language: objective-c
matrix:
    include:
        - osx_image: xcode9.3beta

script:
    - xcodebuild build -project SlackTest.xcodeproj -scheme FeedbackSlack -destination "platform=iOS Simulator,name=iPhone X"

